experiment:
  name: best_segformer

model:
  name: segformer-b0
  num_classes: 5

training:
  epochs: 30
  batch_size: 16
  learning_rate: 0.00005
  weight_decay: 0.00001
  seed: 42
  num_workers: 2

data:
  image_size: 1024

dataset:
  train_subset_size: null

loss:
  dice_weight: 0.5
  ce_weight: 0.5

inference:
  confidence_threshold: 0.5