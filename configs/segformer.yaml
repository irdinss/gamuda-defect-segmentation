experiment:
  name: exp002_30epochs_alldataset

model:
  name: segformer-b0
  num_classes: 5

training:
  epochs: 15
  batch_size: 16
  learning_rate: 0.0001
  weight_decay: 0.00001
  seed: 42
  num_workers: 2

data:
  image_size: 512

dataset:
  train_subset_size: null

loss:
  dice_weight: 0.5
  ce_weight: 0.5

inference:
  confidence_threshold: 0.5